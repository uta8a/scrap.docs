baseURL = 'https://scrap.docs.uta8a.net/'
languageCode = 'ja'
defaultContentLanguage = 'ja'
title = '雑メモ'
theme = 'suuri'

[markup.goldmark.renderer]
hardWraps = true # 改行でbrを入れる
unsafe = true # Markdown中のHTMLタグをrender

[markup.highlight]
noClasses = false # disable injecting style for syntax highlight
